#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@ikas/storefront-cmd/build/ikas.cjs" "$@"
  ret=$?
else 
  node  "$basedir/../@ikas/storefront-cmd/build/ikas.cjs" "$@"
  ret=$?
fi
exit $ret
