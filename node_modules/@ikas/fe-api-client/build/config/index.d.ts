import { GraphQLError } from "graphql";
import { APIErrorCode } from "src/utils/api";
export declare type ConfigType = {
    URL?: string;
    TOKEN?: string;
    HEADERS?: Record<string, string>;
    ON_ERROR?: OnErrorType;
};
export declare type OnErrorType = {
    errorCodes?: ErrorCodeType[];
    operations?: OperationType[];
};
export declare type ErrorCodeType = {
    errorCode: APIErrorCode;
    callback: (cb: CallbackType) => Promise<void> | void;
    refreshQuery?: boolean;
};
export declare type OperationType = {
    operationName: string;
    callback: (cb: CallbackType) => Promise<void> | void;
    refreshQuery?: boolean;
};
export declare type CallbackType = {
    graphQLErrors: GraphQLError[];
    operationName: string;
};
export default class Config {
    static URL: string;
    static TOKEN: string;
    static HEADERS: Record<string, string>;
    static ON_ERROR: OnErrorType;
}
