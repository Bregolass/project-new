import { IkasThemeJsonPageType } from "@ikas/storefront-models";
import { PageInfo } from "./info";
export declare class PageGenerator {
    static generate(workingDir?: string): Promise<boolean>;
    static generatePageWithInfo(pageInfo: PageInfo, workingDir?: string): Promise<boolean>;
    static generatePageWithType(pageType: IkasThemeJsonPageType): Promise<boolean>;
}
