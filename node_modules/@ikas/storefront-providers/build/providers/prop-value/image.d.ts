import { IkasImage } from "@ikas/storefront-models";
import IkasPropValueProvider from ".";
import { IkasImagePropValue } from "../../models/prop-value/image";
export declare class IkasImagePropValueProvider implements IkasPropValueProvider<IkasImage> {
    private imagePropValue;
    private isObject;
    constructor(imagePropValue: string | IkasImagePropValue);
    getValue(): Promise<IkasImage>;
}
