import { IkasImage } from "@ikas/storefront-models";
import IkasPropValueProvider from ".";
import { IkasImagePropValue } from "../../models/prop-value/image";
export declare class IkasImageListPropValueProvider implements IkasPropValueProvider<IkasImage[]> {
    private imageListPropValue;
    constructor(imageListPropValue: (string | IkasImagePropValue)[]);
    getValue(): Promise<IkasImage[]>;
}
