declare type Props = {
    visible: boolean;
    text: string;
    onClose: () => void;
};
declare const ModalResponse: (({ visible, text, onClose }: Props) => JSX.Element) & {
    displayName: string;
};
export default ModalResponse;
