import { IkasImage } from "../../image";
import { IkasVariantValue as IVariantValue } from "@ikas/storefront-models";
export declare class IkasVariantValue implements IVariantValue {
    id: string;
    name: string;
    colorCode: string | null;
    thumbnailImageId: string | null;
    variantTypeId: string;
    thumbnailImage?: IkasImage | null;
    constructor(data: IVariantValue);
    get slug(): string;
}
